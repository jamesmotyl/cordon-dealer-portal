# Project instructions

- Before adding new files or functions, check if something similar already exists in the codebase and extend/reuse it instead of duplicating.
- Keep components and functions small and single-purpose.
- Do not add new libraries/dependencies unless there's no reasonable way to do it with what's already installed — ask first if one seems needed.
- After finishing a task, briefly list any files created or changed.
